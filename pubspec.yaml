name: hain
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+2

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  url_launcher: ^6.1.5
  flutter_launcher_icons: ^0.10.0
  cloud_firestore: ^3.4.8
  firebase_core: ^1.22.0
  firebase_auth: ^3.9.0
  toast: ^0.2.9
  fluttertoast: ^8.0.9
  get: ^4.6.5
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/bamboo.jpg"
  min_sdk_android: 21
